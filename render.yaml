services:
  - type: web
    name: gate-port-codes
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT --workers 1 --timeout 120
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: PYTHON_VERSION
        value: "3.12.0"
      - key: DATABASE_URL
        fromDatabase:
          name: gate-codes-db
          property: connectionString
    healthCheckPath: /api/health

databases:
  - name: gate-codes-db
    plan: free
